pub const ExperimentalEngine = struct {
    pub fn init() ExperimentalEngine {
        return .{};
    }

    // TODO(experimental engine): future HTML, CSS, DOM, layout, and paint work belongs here.
    // Do not use this in the MVP browser shell.
};
